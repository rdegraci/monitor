"""Full-screen TUI front-end for src/monitor/ (opt-in via --tui).

Currently only the throwaway spike (spike.py) lives here — see
docs/cache/PLAN_MONITOR_TUI.md. The REPL remains the default front-end.
"""
