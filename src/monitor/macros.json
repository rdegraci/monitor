{
    "_trust_model": "TRUST MODEL: this file is executable, not configuration. {{tcl ...}} macros run arbitrary Tcl code in a tkinter.Tcl() interpreter on the host; Tcl can `exec` shell commands, read/write files, and access env vars. Only add macros from sources you trust as much as a shell script. Macros added at runtime via `<name=value` are ephemeral but equally unsandboxed.",
    "sample_tcl_macro": "{{tcl puts \"$env(HOME)\"}}",
    "load_context": "Examine the app.py or main.py or server.py file and associated source files",
    "trace": "Given the specified function name, produce a readable downstream call chain (call tree) for that function. The call tree should recursively show all functions that are called, directly or indirectly, from it. Please ignore standard library or system API calls (focus only on user-defined/project functions).\n\n        **Special Request:**  \n\n        - If the call tree is very large, please provide only the first 2–3 levels (or as much as fits in a reasonable output).\n        - Summarize deep branches with a note like \"... (N more calls)\" or \"continued\".\n        - If any branch is recursive, indicate where recursion occurs.\n        - Optionally, ask me if I want details of a particular branch.\n\n        **Example Desired Output:**\n\n        ```\n        myFunction (in MyClass.swift)\n        ├─> validateInput\n        │    └─> logError\n        ├─> fetchData\n        │    ├─> makeNetworkRequest\n        │    ├─> parseResponse\n        │    │    └─> ... (more branches, truncated)\n        │    └─> handleError\n        └─> updateUI\n            └─> ... (more branches, truncated)\n        ```\n\n        Function Name: \"",
    "rank_improvements": "Rank the areas of improvement by ease and impact, with the first being the easiest. Display the output as a simple table",
    "review_changes": "Source code files have been updated. {{do_diff}} then examine each source file noted in the diff. Tell me what do you think about the changes. If there are bugs provide a bug report, ordered such that the easiest to solve first.",
    "order_swift_file": "Update the Swift class so that all properties and methods are grouped and ordered according to the following guidelines:\n\n    1. IMPORTANT: Do not add comments and do not remove existing comments.\n    2. All `public` and `internal` `@Published` variables should appear first, grouped together and sorted alphabetically.\n    3. Next, all other `public` and `internal` variables (non-@Published) should be listed, sorted alphabetically.\n    4. Next, all other `public` and `internal` read only properties\n    5. Next, all private(set) variables\n    6. After public/internal vars, list all `private let` variables, sorted alphabetically.\n    7. Then, list all other `private` variables, sorted alphabetically.\n    8. Next, all other `public` and `internal` read only properties\n    9. All `public` and `internal` functions should appear next (above any private functions), sorted alphabetically.\n    10. All `private` functions should appear last, sorted alphabetically.\n    11. Maintain this access control and ordering scheme throughout the entire class.\n    12. Do not change any logic or functionality, only the order.\n\n    The class should also follow this SwiftLint specification for type content ordering:\n    ```\n    type_contents_order:\n      order:\n        - case\n        - type_alias\n        - associated_type\n        - subtype\n        - type_property\n        - instance_property\n        - ib_inspectable\n        - ib_outlet\n        - initializer\n        - deinitializer\n        - type_method\n        - view_life_cycle_method\n        - ib_action\n        - other_method\n        - subscript\n    ```\n\n    Here is an example ordering for illustration:\n\n    ```swift\n    class ExampleClass {\n        enum State { ... }\n\n        @Published var count: Int\n        @Published var isActive: Bool\n\n        var items: [String]\n        var name: String\n\n        var pairNameItems: [String] { ... }\n\n        private let defaultName: String\n\n        private var cache: [String: Any]\n        private var isLoaded: Bool\n\n        private var filteredNameItems: [String] { ... }\n\n        func fetchItems() { ... }\n        func setup() { ... }\n\n        private func clearCache() { ... }\n        private func validate() { ... }\n    }\n    ```\n\n    IMPORTANT:\n    - Do not change any logic or functionality, only the order.\n    - Do not add comments and do not remove existing comments.\n\n    Path to class:\n    \"",
    "check_refactor_constants": "{{xdiff}} HEAD; Make sure that the constants have not changed values",
    "fixf": "Provide suggested fixes for the function, in diff format, ordered by simplest first:",
    "refactorf": "Provide suggested refactors for the function, in diff format, ordered by simplest first:",
    "test_plan": "Create a comprehensive test plan for the given function or feature. Include unit tests, integration tests, and edge cases. Prioritize high-impact tests first. Output as a numbered list with code snippets in diff format.\n Given:",
    "doc_gen": "Generate or update docstrings/comments for the given code snippet or file. Use Doc Comment for Swift, preferring /// comments. Use Google/Numpy style for Python. Ensure coverage of params, returns, and examples. Output diffs showing additions only.\n Given:",
    "security_audit": "Audit given code for security vulnerabilities: check for SQL injection, XSS, hard-coded secrets, or unsafe deserialization. Reference OWASP top 10. Provide risks ranked by severity, with mitigation diffs.\n Given:",
    "perf_optimize": "Analyze the given function or module for performance bottlenecks (e.g., O(n) loops, redundant calls). Suggest optimizations in diff format, ordered by biggest speedup first. Include time/space complexity notes.\n Given:",
    "branch_status": "Examine the current branch status using perform_git_status. Summarize uncommitted changes, ahead/behind counts, and suggest next actions (e.g., commit, rebase, push). Output as a bullet-point report",
    "todo_extract": "Scan the code for TODO/FIXME comments or unresolved issues. List them by file/line, prioritize by urgency, and suggest resolutions as a table. {{rank_improvements}} for prioritization",
    "multi_file_refactor": "Propose refactors across multiple files (e.g., rename a shared function or update API). {{do_diff}} and list affected files, provide unified diffs, and ensure consistency. Warn on breaking changes ",
    "deploy_check": "Review changes for deployment readiness: check for new deps, env vars, migration needs, or config updates. {{do_diff}} and suggest a deployment checklist as a Markdown table",
    "rank_examine": "Given the specified type, {{rank_improvements}} Type: ",

    "branch": "{{tcl if {[catch {exec git branch --show-current} b]} {puts \"(not in git)\"} else {puts $b}}}",
    "sha": "{{tcl if {[catch {exec git rev-parse --short HEAD} s]} {puts \"(not in git)\"} else {puts $s}}}",
    "git_clean": "{{tcl if {[catch {exec git rev-parse --is-inside-work-tree}]} {puts \"(not in git)\"} elseif {[catch {exec git diff --quiet}]} {puts \"DIRTY\"} else {puts \"clean\"}}}",
    "recent_commits": "Recent commits:\n{{tcl if {[catch {exec git log --oneline -10} c]} {puts \"(not in git or no commits)\"} else {puts $c}}}",
    "branch_age": "{{tcl if {[catch {exec git log -1 --pretty=format:%ar} a]} {puts \"(no commits)\"} else {puts $a}}}",
    "is_git_repo": "{{tcl if {[catch {exec git rev-parse --is-inside-work-tree}]} {puts \"not in a git repo\"} else {puts \"in a git repo\"}}}",

    "today": "{{tcl puts [clock format [clock seconds] -format {%Y-%m-%d}]}}",
    "now": "{{tcl puts [clock format [clock seconds] -format {%Y-%m-%d %H:%M}]}}",
    "weekday": "{{tcl puts [clock format [clock seconds] -format {%A}]}}",

    "cwd": "{{tcl puts [pwd]}}",
    "home": "{{tcl if {[info exists env(HOME)]} {puts $env(HOME)} else {puts \"(HOME not set)\"}}}",
    "hostname": "{{tcl puts [info hostname]}}",

    "py_project": "{{tcl if {[file exists pyproject.toml]} {puts \"pyproject.toml-based Python project\"} elseif {[file exists setup.py]} {puts \"setup.py-based Python project\"} elseif {[file exists requirements.txt]} {puts \"requirements.txt-based Python project\"} else {puts \"no Python project markers in cwd\"}}}",
    "py_version": "{{tcl if {[catch {exec python --version} v]} {puts \"python not on PATH\"} else {puts $v}}}",
    "py_deps_head": "{{tcl if {[catch {set f [open requirements.txt r]} err]} {puts \"no requirements.txt\"} else {set out [list]; set i 0; while {$i < 20 && [gets $f line] >= 0} {lappend out $line; incr i}; close $f; puts [join $out \"\n\"]}}}",
    "py_files": "{{tcl puts [llength [glob -nocomplain -directory . -tails -type f *.py **/*.py]]}}",

    "ios_xcode_version": "{{tcl if {[catch {exec xcodebuild -version} v]} {puts \"Xcode not installed or not on PATH\"} else {puts $v}}}",
    "ios_swift_version": "{{tcl if {[catch {exec swift --version} v]} {puts \"swift not on PATH\"} else {puts [lindex [split $v \"\n\"] 0]}}}",
    "ios_project_files": "{{tcl set p [glob -nocomplain *.xcodeproj *.xcworkspace Package.swift]; if {$p eq {}} {puts \"no Xcode/SPM project in cwd\"} else {puts $p}}}",
    "ios_swift_files_count": "{{tcl if {[catch {exec find . -name {*.swift} -type f} files]} {puts \"(unable to count)\"} else {puts [llength [split [string trim $files] \"\n\"]]}}}",
    "ios_booted_simulator": "{{tcl if {[catch {exec xcrun simctl list devices booted} d]} {puts \"simctl unavailable (Xcode not installed?)\"} else {puts $d}}}",
    "ios_schemes": "{{tcl if {[catch {exec xcodebuild -list} l]} {puts \"no xcodebuild project in cwd\"} else {puts $l}}}",
    "ios_pkg_swift_head": "{{tcl if {[catch {set f [open Package.swift r]} err]} {puts \"no Package.swift\"} else {set out [list]; set i 0; while {$i < 30 && [gets $f line] >= 0} {lappend out $line; incr i}; close $f; puts [join $out \"\n\"]}}}",

    "debug_ctx": "{{tcl if {[info exists env(DEBUG_PROMPT)] && $env(DEBUG_PROMPT) eq \"1\"} {puts \"DEBUG MODE: extra verbose\"} else {puts \"\"}}}",

    "branch_diff": "On branch {{branch}}. {{do_diff}} {{rank_improvements}}",
    "context_audit": "Date: {{today}}. Project: {{py_project}}. Branch: {{branch}}. {{branch_status}}",
    "commit_with_age": "Latest commit ({{branch_age}}): {{tcl if {[catch {exec git log -1 --pretty=format:%s} m]} {puts \"(no commits)\"} else {puts $m}}}"
}
